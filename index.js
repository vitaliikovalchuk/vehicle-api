import express from 'express';

const app = express();

const PORT = process.env.PORT || 3000;
vitalii; kovalchul; one nore;

app.get('/', (req, res) => {
    res.send()
})

app.listen(PORT, () => {
    console.log('Server is running...')
}) 